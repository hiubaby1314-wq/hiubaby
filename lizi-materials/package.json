{
  "name": "lizi-materials",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "node -e \"try{const fs=require('fs');const p='./node_modules/edge-tts/package.json';const d=JSON.parse(fs.readFileSync(p));d.main='out/index.js';fs.writeFileSync(p,JSON.stringify(d,null,2));console.log('Fixed edge-tts');}catch(e){}\""
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "better-sqlite3": "^11.10.0",
    "express": "^4.18.0",
    "multer": "^1.4.5-lts.1"
  }
}